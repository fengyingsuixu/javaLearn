spring:
  application:
    name: configServer
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/fengyingsuixu/javaLearn
          searchPaths: configFile
          username:
          password:
server:
  port: 8000
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7000/eureka/



spring:
  application:
    name: configServer
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/fengyingsuixu/javaLearn
          searchPaths: configFile
          username:
          password:
server:
  port: 8000


